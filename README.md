# LessonFlow

这是一个基于 Vue 3 + Vite 的教材编辑器项目。

## 功能特性

- 📝 可视化教材编辑
- 🎨 现代化 UI 设计
- 📱 响应式布局
- ⚡ 快速开发体验
- 🚀 Vite 构建工具
- 🎯 Vue 3 Composition API
- 🎨 Element Plus UI 组件库

## 技术栈

- **框架**: Vue 3
- **构建工具**: Vite
- **语言**: TypeScript
- **UI 组件库**: Element Plus
- **样式**: Tailwind CSS
- **路由**: Vue Router
- **状态管理**: Pinia
- **图标**: Element Plus Icons

## 开始使用

1. 安装依赖

```bash
npm install
# 或
pnpm install
```

2. 启动开发服务器

```bash
npm run dev
# 或
pnpm dev
```

3. 打开浏览器访问 [http://localhost:3000](http://localhost:3000)

## 项目结构

```
src/
├── components/         # 可复用组件
│   └── Layout.vue     # 布局组件
├── pages/             # 页面组件
│   ├── HomePage.vue   # 首页
│   └── EditorPage.vue # 编辑器页面
├── router/            # 路由配置
│   └── index.ts       # 路由定义
├── App.vue            # 根组件
├── main.ts            # 应用入口
└── index.css          # 全局样式
```

## 开发指南

- 使用 `pnpm lint` 检查代码规范
- 使用 `pnpm lint:fix` 自动修复代码问题
- 使用 `pnpm format` 格式化代码
- 使用 `pnpm type-check` 进行类型检查
- 使用 `pnpm build` 构建生产版本
- 使用 `pnpm preview` 预览构建结果

## 贡献

欢迎提交 Issue 和 Pull Request！
